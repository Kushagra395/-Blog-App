<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Minipro</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class=" px-7 w-screen min-h-screen bg-zinc-900 text-white">
        <div class="w-full flex justify-end">
            <a href="/logout" class="px-2 pt-2 mb-2  text-red-400 hover:text-red-800">Logout</a>  
        </div>
        <div class="flex gap-5">
            <div class=" my-10 h-13 w-13   rounded-full overflow-hidden"  >
                <img class="w-full h-full object-cover  " src= '/images/uploads/<%= user.profile %> 'alt="user image"/>
            </div>
            <h3 class="   my-12 text-4xl"> Hello, <%=user.name%> ☺️</h3>
        </div>
        
        <h2 class="    text-zinc-200 font-light"> willing to post something?</h2>
        <form action="/post" method="post">
            <textarea  placeholder=" What's new in mind?" class=" p-2 pb-1 resize-none   w-1/3 block  bg-transparent border-zinc-700 border-2 my-4 mb-2 rounded-lg" name="content"></textarea>
            <button class="  h-10 w-40 bg-blue-500 hover:bg-blue-700 rounded-lg text-white rounded-l block  font-light">Create New post</button>
            
        </form>


        <div class="postsblock mt-25">
          <h2 class=" text-xl text-zinc-400 ">Your posts</h2>

          <div class="post-container flex flex-wrap gap-9 mt-5">
            <% user.post.reverse().forEach(function(post){ %>
              <div class="post w-2/7 pt-2 p-3 px-1 border-2 bg-zinc-600 border-zinc-500 rounded-lg">
                    <h4 class=" text-blue-200 font-bold">@<%=user.username%></h4>
                    <p class="font-light"> <%=post.content%> </p>
                    <div class="btn flex mt-4 gap-4">
                    <a class="text-blue-400" href="/like/<%=post._id%>">
                        <%= post.likes.indexOf(user._id) ? 'like' : 'unlike'%>
                    </a>
                    <a class="text-zinc-400" href="/edit/<%=post._id%>">edit</a>
                    <a class="text-red-400" href="/delete/<%=post._id%>">delete</a>
                    <small class=" mt-1 text-bold ml-45 text-yellow-200"><%=post.likes.length%> likes</small>
    
                   </div>
                   
    
                  </div>
             <%})%>
              
           </div>
        

    </div>
</body>
</html>